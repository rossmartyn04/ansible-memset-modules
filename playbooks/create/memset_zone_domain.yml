- hosts: localhost
  become: false

  tasks:
  - name: include secrets
    include_vars: ../secrets.yaml

  - name: add domain to zone
    local_action:
      module: memset_zone_domain
      api_key: "{{ api_key }}"
      state: present
      domain: "{{ zone_domain }}"
      zone: "{{ zone_domain }}"
